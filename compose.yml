services:
  redu-demo:
    build:
      context: .
      dockerfile: Containerfile
    container_name: redu-demo
    restart: unless-stopped
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
    ports:
      - "3000:3000"
